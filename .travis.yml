language: php

php:
  - 7.0
  - 7.1

sudo: required
dist: trusty

before_install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install valgrind

script:
  - make -j4 && ./.travis/run-tests.sh

after_failure:
 - sudo apt-get -qq install gdb
 - ./.travis/after_failure.sh
